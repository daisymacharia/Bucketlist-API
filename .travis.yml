language: python
python:
    - 3.4
install:
    - pip install -r requirements.txt
    - pip install coveralls
services:
  - postgresql
before_script:
  - psql -c 'create database test_bucketlist_api;' -U postgres

script: nosetests --with-coverage

after_success:
    - coveralls
